<UserControl
    x:Class="CaeloWorks.NINA.Benchmark.View.SystemInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="220" d:DesignWidth="380">
    <UserControl.Resources>
        <!-- Tabler "refresh" (MIT), stroked to match the button text colour. -->
        <StreamGeometry x:Key="CaeloIcon_Refresh">M20 11a8.1 8.1 0 0 0 -15.5 -2m-.5 -4v4h4 M4 13a8.1 8.1 0 0 0 15.5 2m.5 4v-4h-4</StreamGeometry>
        <Style x:Key="IconPath" TargetType="Path">
            <Setter Property="Width" Value="15" />
            <Setter Property="Height" Value="15" />
            <Setter Property="Stretch" Value="Uniform" />
            <Setter Property="StrokeThickness" Value="2" />
            <Setter Property="StrokeStartLineCap" Value="Round" />
            <Setter Property="StrokeEndLineCap" Value="Round" />
            <Setter Property="StrokeLineJoin" Value="Round" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Stroke" Value="{DynamicResource ButtonForegroundBrush}" />
        </Style>
    </UserControl.Resources>
    <!-- DataContext is expected to be a BenchmarkEngine -->
    <GroupBox Header="System information" Margin="5">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock
                Grid.Row="0"
                FontFamily="Consolas,Courier New"
                TextWrapping="NoWrap"
                Text="{Binding SystemInfo.Summary, FallbackValue='Collecting…', TargetNullValue='Collecting…'}" />
            <Button
                Grid.Row="1"
                HorizontalAlignment="Left"
                Margin="0,8,0,0"
                Padding="16,6,16,6"
                HorizontalContentAlignment="Left"
                Command="{Binding RefreshSystemInfoCommand}">
                <StackPanel Orientation="Horizontal" Margin="10,3">
                    <Path Style="{StaticResource IconPath}" Data="{StaticResource CaeloIcon_Refresh}" />
                    <TextBlock Margin="7,0,0,0" VerticalAlignment="Center" Text="Refresh" />
                </StackPanel>
            </Button>
        </Grid>
    </GroupBox>
</UserControl>
