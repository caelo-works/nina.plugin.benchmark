<UserControl
    x:Class="CaeloWorks.NINA.Benchmark.View.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="180" d:DesignWidth="520">
    <!-- DataContext is expected to be a BenchmarkEngine -->
    <GroupBox Header="Sharing (submit results online)" Margin="5">
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,3,10,3" Text="Machine name" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,3" Text="{Binding MachineName, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Optional label for this rig. Defaults to the active profile name if left empty." />

            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="#90A0B8">
                <Run Text="The Share button on a run submits it to the public site at" />
                <Run Text="{Binding SubmissionEndpoint, Mode=OneWay}" FontFamily="Consolas,Courier New" />
                <Run Text="anonymously, with no sign-up. Sent: your machine specs (CPU, GPU, RAM, OS, power plan) and the per-function timings. No images. Runs are labelled with your N.I.N.A. Observer name and the machine name above." />
            </TextBlock>
        </Grid>
    </GroupBox>
</UserControl>
